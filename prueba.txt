start MiPrograma {
    // Declaracion de variables y constantes
    @edad int;
    @if int;
    #gravedad float = 9.81;
  
    // Asignacion
    edad = 20;

    // Imprimir en consola
    aqhe("Iniciando prueba de Aqheran...");

    // Estructura Condicional
    if (edad >= 18) {
        aqhe("El usuario es mayor de edad.");
    } else {
        aqhe("El usuario es menor de edad.");
    }

    // Estructura Iterativa (While)
    @contador int;
    contador = 1;
    
    whhile (contador <= 3) {
        aqhe("Contador: " & contador);
        contador += 1;
    }
    readline(edad);
    aqhe(edad & "es mayor de edad");
} end